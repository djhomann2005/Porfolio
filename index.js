function showDescription(projectElement) {
    const description = projectElement.querySelector(".desc-container");
    description.style.display = "block";
}

function hideDescription(projectElement) {
    const description = projectElement.querySelector(".desc-container");
    description.style.display = "none";
}